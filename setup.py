from setuptools import setup

setup(
   name='Simion_package',
   version='0.9.1',
   description='A simple module for creating complex Simion gem files and possibly easily rescaling them.',
   author='Zdenek Vostrel',
   packages=['Simion_package'],
   install_requires=['typing'],
)